name: Claims Service CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  JAVA_VERSION: '17'
  # TODO: Add your Docker registry and image name here
  # REGISTRY: ghcr.io
  # IMAGE_NAME: ${{ github.repository }}

jobs:

  # ─── JOB 1: BUILD AND TEST ────────────────────────────────────────────────
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # TODO: Add setup-java action
      # Use: actions/setup-java@v4
      # Configure: java-version, distribution (temurin), cache (maven)
      - name: Set up Java
        # YOUR COPILOT PROMPT: Complete this step with actions/setup-java@v4 for Java 17 with Maven cache

      # TODO: Add Maven build step (compile only, skip tests)
      - name: Build with Maven
        # YOUR COPILOT PROMPT: Add run command for: mvn compile -B

      # TODO: Add Maven test step
      - name: Run tests
        # YOUR COPILOT PROMPT: Add run command for: mvn test -B with surefire plugin

      # TODO: Add test report upload
      # Upload surefire-reports as artifact named "test-results"
      - name: Upload test results
        if: always()
        # YOUR COPILOT PROMPT: Upload target/surefire-reports as artifact "test-results"

      # TODO: Add code coverage report (optional)
      # Use jacoco:report and upload target/site/jacoco as artifact

  # ─── JOB 2: DOCKER BUILD AND PUSH ────────────────────────────────────────
  # (Complete after Part A is working)
  docker-build:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: build-and-test
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # TODO: Add setup-java (same as above — needed to build the JAR)

      # TODO: Add Maven package step (mvn package -DskipTests -B)

      # TODO: Set up Docker Buildx
      # Use: docker/setup-buildx-action@v3

      # TODO: Log in to GitHub Container Registry
      # Use: docker/login-action@v3
      # Registry: ghcr.io
      # Username: ${{ github.actor }}
      # Password: ${{ secrets.GITHUB_TOKEN }}

      # TODO: Build and push Docker image
      # Use: docker/build-push-action@v5
      # Context: .
      # Push: true
      # Tags: ghcr.io/${{ github.repository }}:latest and :${{ github.sha }}

  # ─── JOB 3: SONARQUBE QUALITY GATE ───────────────────────────────────────
  # Part C: Add SonarQube analysis (requires SONAR_TOKEN secret)
  sonarqube:
    name: SonarQube Quality Gate
    runs-on: ubuntu-latest
    needs: build-and-test
    # NOTE: Requires repository secret SONAR_TOKEN to be configured

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # SonarQube requires full history

      # TODO: Set up Java with SonarQube cache

      # TODO: Run SonarQube analysis
      # YOUR COPILOT PROMPT:
      # Add a SonarQube analysis step that:
      # - Runs mvn verify sonar:sonar
      # - Sets sonar.projectKey to the repo name
      # - Uses SONAR_TOKEN from secrets
      # - Uses SONAR_HOST_URL (or sonarcloud.io)
      # - Fails the build if quality gate is not passed
